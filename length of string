// Online C compiler to run C program online
//length of string
#include <stdio.h>
#include<string.h>
int main()
{
   int l=0,i;
   char str[100];
   gets(str);
   for(i=0;str[i]!='\0';i++)
   l++;
 printf("%d",l);  
}
