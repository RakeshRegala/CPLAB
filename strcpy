// Online C compiler to run C program online
//copying of string from s2 to s1
#include<stdio.h>
#include<string.h>
int main()
{
    char s1[200],s2[200];
   int i,j;
   gets(s1);
   gets(s2);
   for(i=0,j=0;s2[j]!='\0';i++,j++)
   s1[i]=s2[j];
   s1[i]='\0';
   fputs(s1,stdout);
   //printf("%s",s1); //puts(s1);
}
