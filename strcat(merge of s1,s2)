#include <stdio.h>
#include<string.h>
int main()
{
    char s1[200],s2[200];
    int i,l,j;
    gets(s1);
    gets(s2);
    l=strlen(s1);
    for(i=l,j=0;s2[j]!='\0';i++,j++)
    {
    s1[i]=s2[j];
    }
    s1[i]='\0';    
    puts(s1);
}
