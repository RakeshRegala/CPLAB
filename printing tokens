#include<stdio.h>
#include<string.h>
int main()
{
    char s[1000];
    int i,l;
   gets(s);
    l=strlen(s);
    for(i=0;i<l;i++)
    {
        if(s[i]==' ')
         puts("");
        else
         {
        printf("%c",s[i]);
        }
    }
}
